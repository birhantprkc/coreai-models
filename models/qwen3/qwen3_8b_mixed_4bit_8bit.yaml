# Mixed-precision palettization for Qwen3-8B on iOS.
#
# Global INT4 P4/group-32, with the PPL-sensitive transformer blocks
# (6, 8, 14, 24, 33, 34) promoted to INT8 per-tensor.
kmeans_palettization_config:
  global_config:
    op_state_spec:
      weight:
        n_bits: 4
        granularity:
          type: per_grouped_channel
          axis: 0
          group_size: 32
  module_type_configs:
    torch.nn.modules.sparse.Embedding: null
    coreai_models.primitives.ios.embedding.LoadEmbeddings: null
  module_name_configs:
    'extend\.model\.layers\.(6|8|14|24|33|34)\.(self_attn|mlp)\.(q_proj|k_proj|v_proj|o_proj|gate_proj|up_proj|down_proj)':
      op_state_spec:
        weight:
          n_bits: 8
          granularity:
            type: per_tensor
